# Stage 18 lock refresh trigger

This temporary documentation marker exists only to open the helper pull request that runs the trusted Stage 18 lockfile refresh workflow. It is removed before the helper branch is merged.

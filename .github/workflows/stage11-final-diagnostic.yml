name: Stage 11 Final Lint Diagnostic

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  contents: read

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out implementation branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@9.15.4 --activate

      - name: Capture lint findings
        shell: bash
        run: |
          set +e
          pnpm install --frozen-lockfile > stage11-final-lint.txt 2>&1
          pnpm lint:source >> stage11-final-lint.txt 2>&1
          exit 0

      - name: Upload lint findings
        uses: actions/upload-artifact@v4
        with:
          name: stage11-final-lint
          path: stage11-final-lint.txt
          if-no-files-found: error

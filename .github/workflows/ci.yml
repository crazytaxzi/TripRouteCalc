name: CI

on:
  push:
    branches: [main, 'agent/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verification:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/trip_route_calc_test?schema=public
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable >/dev/null
          corepack prepare pnpm@9.15.4 --activate >/dev/null

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --reporter=silent

      - name: Generate Prisma client
        run: pnpm db:generate >/dev/null

      - name: Capture current typecheck output
        id: typecheck
        shell: bash
        run: |
          set +e
          pnpm typecheck:source > stage16-typecheck-current.txt 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          echo >> stage16-typecheck-current.txt
          echo "EXIT_STATUS=$status" >> stage16-typecheck-current.txt
          exit 0

      - name: Upload current typecheck output
        uses: actions/upload-artifact@v4
        with:
          name: stage16-typecheck-current
          path: stage16-typecheck-current.txt
          if-no-files-found: error
          retention-days: 1

      - name: Preserve typecheck failure
        if: steps.typecheck.outputs.status != '0'
        run: exit 1

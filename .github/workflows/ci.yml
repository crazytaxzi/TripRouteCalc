name: CI

on:
  push:
    branches: [main, 'agent/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verification:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable >/dev/null
          corepack prepare pnpm@9.15.4 --activate >/dev/null

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --reporter=silent

      - name: Capture targeted lint output
        id: lint
        shell: bash
        run: |
          set +e
          pnpm exec eslint packages/foundation/test/confidence.test.ts \
            packages/foundation/test/eta-confidence-explanations.test.ts \
            > stage16-acceptance-lint.txt 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          echo >> stage16-acceptance-lint.txt
          echo "EXIT_STATUS=$status" >> stage16-acceptance-lint.txt
          exit 0

      - name: Upload targeted lint output
        uses: actions/upload-artifact@v4
        with:
          name: stage16-acceptance-lint
          path: stage16-acceptance-lint.txt
          if-no-files-found: error
          retention-days: 1

      - name: Preserve lint failure
        if: steps.lint.outputs.status != '0'
        run: exit 1

name: CI

on:
  push:
    branches: [main, 'agent/**']
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  verification:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/trip_route_calc_test?schema=public
    steps:
      - name: Check out implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/stage-16-confidence-data-quality
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable >/dev/null
          corepack prepare pnpm@9.15.4 --activate >/dev/null

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --reporter=silent

      - name: Generate Prisma client
        run: pnpm db:generate >/dev/null

      - name: Apply safe lint fixes
        id: lint
        shell: bash
        run: |
          set +e
          pnpm exec eslint packages/foundation/src/eta-simulator.ts packages/foundation/src/confidence.ts --fix > stage16-lint-repair.txt 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          echo >> stage16-lint-repair.txt
          echo "EXIT_STATUS=$status" >> stage16-lint-repair.txt
          exit 0

      - name: Commit mechanical lint repairs
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/foundation/src/eta-simulator.ts packages/foundation/src/confidence.ts
          if git diff --cached --quiet; then
            echo "No auto-fixable changes."
          else
            git commit -m "Apply Stage 16 lint repairs"
            git push origin HEAD:agent/stage-16-confidence-data-quality
          fi

      - name: Upload remaining lint output
        uses: actions/upload-artifact@v4
        with:
          name: stage16-lint-repair
          path: stage16-lint-repair.txt
          if-no-files-found: error
          retention-days: 1

      - name: Preserve remaining lint failure
        if: steps.lint.outputs.status != '0'
        run: exit 1

name: CI

on:
  push:
    branches: [main, 'agent/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verification:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/trip_route_calc_test?schema=public
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable >/dev/null
          corepack prepare pnpm@9.15.4 --activate >/dev/null

      - name: Install dependencies quietly
        run: pnpm install --frozen-lockfile --reporter=silent

      - name: Generate Prisma client quietly
        run: pnpm db:generate >/dev/null

      - name: Capture type-check output
        id: typecheck
        shell: bash
        run: |
          set +e
          pnpm typecheck:source > stage15-typecheck.txt 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload type-check diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: stage15-typecheck
          path: stage15-typecheck.txt
          if-no-files-found: error
          retention-days: 1

      - name: Preserve failing result
        if: steps.typecheck.outputs.status != '0'
        run: exit 1
